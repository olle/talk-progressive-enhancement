{
  "repository": {},
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.5.3",
    "grunt": "^0.4.5",
    "grunt-bower-install-simple": "^1.0.3",
    "grunt-cli": "^0.1.13",
    "grunt-contrib-connect": "^0.8.0",
    "grunt-contrib-watch": "^0.6.1",
    "load-grunt-tasks": "^0.6.0"
  },
  "scripts": {
    "postinstall": "bower update && bower install && bower prune",
    "start": "grunt serve"
  }
}
